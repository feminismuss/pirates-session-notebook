hot to create a new repository

Lokal:

git init

git add .

git commit -m "iisignaogh"

mit repository auf github verbinden: git remote add origin <ssh link>

check it the remote reposiory was successfully connected: git remote -v

beim ersten push: git push -u origin main
